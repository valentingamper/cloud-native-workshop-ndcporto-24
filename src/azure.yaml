# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: src
services:  
  app:
    language: dotnet
    project: ./Dometrain.Aspire.AppHost/Dometrain.Aspire.AppHost.csproj
    host: containerapp
